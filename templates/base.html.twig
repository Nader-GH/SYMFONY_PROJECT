{% block title %}Login - RxMarketplace{% endblock %}

{% block body %}
<div class="max-w-md mx-auto">
    <div class="text-center mb-8">
        <h1 class="text-3xl font-bold">Login</h1>
        <p class="text-gray-600 mt-2">Welcome back to RxMarketplace</p>
    </div>
    
    <div class="bg-white rounded-lg shadow-md p-6">
        {% if error %}
            <div class="bg-red-50 text-red-800 p-3 rounded-md mb-4 text-sm">
                {{ error.messageKey|trans(error.messageData, 'security') }}
            </div>
        {% endif %}
        
        <form method="post">
            <div class="mb-4">
                <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email Address</label>
                <input type="email" id="email" name="_username" value="{{ last_username }}" required
                       class="w-full px-3 py-2 border border-gray-300 rounded-md">
            </div>
            
            <div class="mb-4">
                <label for="password" class="block text-sm font-medium text-gray-700 mb-1">Password</label>
                <input type="password" id="password" name="_password" required
                       class="w-full px-3 py-2 border border-gray-300 rounded-md">
            </div>
            
            <button type="submit" class="w-full bg-blue-900 text-white px-4 py-2 rounded hover:bg-blue-800">
                Login
            </button>
        </form>
    </div>
</div>
{% endblock %}
